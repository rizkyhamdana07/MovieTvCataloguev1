<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rizkyhamdana.movietvcatalogue">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieTvCatalogue">
        <activity android:name=".splashscreen.SplashScreenActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tvshow.details.DetailsTvShowActivity"
            android:parentActivityName=".home.HomeActivity"/>
        <activity android:name=".movie.details.DetailsMovieActivity"
            android:parentActivityName=".home.HomeActivity"/>
        <activity
            android:name=".home.HomeActivity"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>